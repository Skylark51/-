import{GAME_CONFIG,FEVER_CONFIG,getDifficultyConfig}from'../../data/game-config.js';import{getTrainingMode}from'../../data/training-modes.js';import{evaluateAnswer}from'./question-engine.js';import{ToadDialogueSelector}from'../../data/dialogues/toad-dialogues.js';
const clamp=(v,min,max)=>Math.max(min,Math.min(max,v));
export class GameCore{constructor({questionEngine,config=GAME_CONFIG,feverConfig=FEVER_CONFIG,eventTarget=globalThis.window,dialogueSelector=new ToadDialogueSelector()}={}){if(!questionEngine)throw new Error('QuestionEngine이 필요합니다.');this.questionEngine=questionEngine;this.config=config;this.feverConfig=feverConfig;this.eventTarget=eventTarget;this.dialogueSelector=dialogueSelector;this.listeners=new Map();this.submissionLocked=false;this.warningLevel=null;this.state=this.initialState()}
initialState(){return{status:'idle',trainingId:null,difficulty:'normal',water:70,score:0,combo:0,correctInStage:0,currentQuestionId:null,questionTimeRemaining:0,reviewMode:false,lastWrongQuestionId:null,startedAt:null,elapsedSeconds:0,lastCorrectAt:null,feverCharge:0,feverActive:false,feverRemaining:0,feverCount:0,lastResponseMs:0,stageIndex:0}}
on(type,listener){const set=this.listeners.get(type)||new Set();set.add(listener);this.listeners.set(type,set);return()=>set.delete(listener)}emit(type,detail={}){const payload={...detail,state:this.snapshot()};for(const listener of this.listeners.get(type)||[])listener(payload);if(this.eventTarget?.dispatchEvent&&typeof CustomEvent!=='undefined')this.eventTarget.dispatchEvent(new CustomEvent(type,{detail:payload}))}
get training(){return getTrainingMode(this.state.trainingId)}get stage(){const mode=this.training;return mode?{id:mode.id,name:mode.title,description:mode.description,leakPerSecond:mode.rules.leakPerSecond,timeLimit:15}:null}get question(){return this.questionEngine.getQuestion(this.state.currentQuestionId)}difficultyConfig(){return getDifficultyConfig(this.state.difficulty)}timeLimit(){const base=this.state.difficulty==='hard'?12:this.state.difficulty==='easy'?20:15;return base*this.difficultyConfig().timeFactor}leakPerSecond(){const base=this.training?.rules.leakPerSecond||0;return base*this.difficultyConfig().leakFactor*(this.state.feverActive?this.feverConfig.leakMultiplier:1)}
snapshot(){return{...this.state,correctAnswersPerStage:this.config.correctAnswersToClear,stageId:this.state.trainingId,fever:{active:this.state.feverActive,charge:this.state.feverCharge,remaining:this.state.feverRemaining,count:this.state.feverCount}}}
start({trainingId,difficulty='normal',resumeState=null,reviewMode=false,questionId=null}={}){const mode=getTrainingMode(trainingId);if(!mode)throw new Error(`알 수 없는 훈련 ID: ${trainingId||'(없음)'}`);this.state=this.initialState();this.state.trainingId=mode.id;this.state.difficulty=mode.difficultyLevels.includes(difficulty)?difficulty:'normal';this.state.reviewMode=Boolean(reviewMode);this.state.water=mode.rules.initialWater;if(resumeState?.trainingId===mode.id){for(const key of['water','score','combo','correctInStage','feverCount'])if(Number.isFinite(Number(resumeState[key])))this.state[key]=Number(resumeState[key]);this.state.water=clamp(this.state.water,1,this.config.maxWater)}this.state.status='running';this.state.startedAt=new Date().toISOString();this.nextQuestion(questionId);this.emit('training:start',{training:mode,resumed:Boolean(resumeState)});this.emit('game:start',{difficulty:this.state.difficulty,resumed:Boolean(resumeState),stage:this.stage,training:mode});return this.snapshot()}
nextQuestion(preferredId=null){let question=preferredId?this.questionEngine.getQuestion(preferredId):null;if(!question||question.trainingId!==this.state.trainingId)question=this.questionEngine.next({trainingId:this.state.trainingId,difficultyRange:this.difficultyConfig().difficultyRange,reviewMode:this.state.reviewMode});this.state.currentQuestionId=question.id;this.state.questionTimeRemaining=this.timeLimit();return question}
tick(deltaSeconds){if(this.state.status!=='running')return this.snapshot();const delta=clamp(Number(deltaSeconds)||0,0,this.config.maxDeltaSeconds);this.state.elapsedSeconds+=delta;this.state.water=clamp(this.state.water-this.leakPerSecond()*delta,0,this.config.maxWater);this.state.questionTimeRemaining=Math.max(0,this.state.questionTimeRemaining-delta);if(this.state.feverActive){this.state.feverRemaining=Math.max(0,this.state.feverRemaining-delta);if(this.state.feverRemaining<=0)this.endFever('expired')}this.checkWaterWarnings();if(this.state.water<=0)return this.over('water_empty');if(this.state.questionTimeRemaining<=0)this.timeout();return this.snapshot()}
checkWaterWarnings(){const level=this.state.water<=10?'critical':this.state.water<=50?'warning':null;if(level&&level!==this.warningLevel){this.emit(`water:${level}`,{water:this.state.water});if(level==='critical')this.speak('waterCritical')}this.warningLevel=level}
chargeFever(){const now=this.state.elapsedSeconds;const within=this.state.lastCorrectAt!=null&&now-this.state.lastCorrectAt<=this.feverConfig.answerWindowSeconds;this.state.feverCharge=within?this.state.feverCharge+1:1;this.state.lastCorrectAt=now;this.emit('fever:charge',{charge:this.state.feverCharge,required:this.feverConfig.requiredCombo});if(this.state.feverCharge>=this.feverConfig.requiredCombo)this.startFever()}
startFever(){if(this.state.feverActive)return;this.state.feverActive=true;this.state.feverRemaining=this.feverConfig.durationSeconds;this.state.feverCount+=1;this.state.feverCharge=0;this.emit('fever:start',{duration:this.feverConfig.durationSeconds,scoreMultiplier:this.feverConfig.scoreMultiplier,waterGainMultiplier:this.feverConfig.waterGainMultiplier,leakMultiplier:this.feverConfig.leakMultiplier});this.speak('feverStart')}
extendFever(){this.state.feverRemaining=Math.min(this.config.feverMaxSeconds,this.state.feverRemaining+this.config.feverExtendSeconds);this.emit('fever:extend',{remaining:this.state.feverRemaining,added:this.config.feverExtendSeconds})}
endFever(reason='ended'){if(!this.state.feverActive)return;this.state.feverActive=false;this.state.feverRemaining=0;this.state.feverCharge=0;this.emit('fever:end',{reason})}
resetFever(reason){this.state.lastCorrectAt=null;this.state.feverCharge=0;if(this.state.feverActive)this.endFever(reason);else this.emit('fever:charge',{charge:0,required:this.feverConfig.requiredCombo})}
speak(category){const line=this.dialogueSelector.pick(category);this.emit('toad:speak',line);return line}
submit(input){if(this.submissionLocked||this.state.status!=='running')return{accepted:false,reason:this.state.status};if(String(input??'').trim()==='')return{accepted:false,reason:'empty'};this.submissionLocked=true;try{const question=this.question;const result=evaluateAnswer(question,input);this.questionEngine.recordResult(question,result.correct);const responseMs=Math.max(0,Math.round((this.timeLimit()-this.state.questionTimeRemaining)*1000));this.state.lastResponseMs=responseMs;if(result.correct)this.correct(question,responseMs);else this.wrong(question,'wrong',responseMs);return{accepted:true,...result,question,responseMs}}finally{this.submissionLocked=false}}
correct(question,responseMs){this.state.combo+=1;this.state.correctInStage+=1;if(this.state.feverActive)this.extendFever();else this.chargeFever();const difficulty=this.difficultyConfig();const scoreMultiplier=this.state.feverActive?this.feverConfig.scoreMultiplier:1;const waterMultiplier=this.state.feverActive?this.feverConfig.waterGainMultiplier:1;const waterGain=this.training.rules.correctWaterGain*difficulty.gainFactor*waterMultiplier;const raw=this.config.baseCorrectScore+this.state.combo*this.config.comboScoreBonus+Math.round(this.state.questionTimeRemaining*this.config.timeScoreMultiplier);const scoreGain=Math.round(raw*scoreMultiplier);this.state.water=clamp(this.state.water+waterGain,0,this.config.maxWater);this.state.score+=scoreGain;this.emit('answer:correct',{question,waterGain,combo:this.state.combo,scoreGain,responseMs,fever:this.state.feverActive});const category=this.state.feverActive?'feverCorrect':responseMs<=this.feverConfig.answerWindowSeconds*1000?'fastCorrect':this.state.combo>=3?'combo':'normalCorrect';this.speak(category);if(this.state.correctInStage>=this.config.correctAnswersToClear)this.clearTraining();else this.nextQuestion()}
wrong(question,reason,responseMs){const penalty=this.training.rules.wrongWaterPenalty*this.difficultyConfig().penaltyFactor;this.state.water=clamp(this.state.water-penalty,0,this.config.maxWater);this.state.combo=0;this.state.lastWrongQuestionId=question?.id||null;this.resetFever(reason);this.emit('answer:wrong',{question,waterPenalty:penalty,reason,responseMs});if(this.state.water<=0)this.over('water_empty');else this.nextQuestion()}
timeout(){if(this.state.status!=='running')return;const question=this.question;const penalty=this.training.rules.timeoutWaterPenalty*this.difficultyConfig().penaltyFactor;this.questionEngine.recordResult(question,false);this.state.water=clamp(this.state.water-penalty,0,this.config.maxWater);this.state.combo=0;this.state.lastWrongQuestionId=question?.id||null;this.resetFever('timeout');this.emit('answer:timeout',{question,waterPenalty:penalty,responseMs:this.timeLimit()*1000});if(this.state.water<=0)this.over('water_empty');else this.nextQuestion()}
clearTraining(){this.state.status='cleared';this.endFever('clear');this.emit('training:clear',{training:this.training,score:this.state.score});this.speak('gameClear');this.emit('game:clear',{score:this.state.score,training:this.training})}
pause(){if(this.state.status!=='running')return false;this.state.status='paused';this.emit('game:pause');return true}resume(){if(this.state.status!=='paused')return false;this.state.status='running';this.emit('game:resume');return true}togglePause(){return this.state.status==='paused'?this.resume():this.pause()}over(reason='ended'){if(['over','cleared'].includes(this.state.status))return this.snapshot();this.state.status='over';this.endFever('game_over');this.emit('game:over',{reason,score:this.state.score,training:this.training});return this.snapshot()}}