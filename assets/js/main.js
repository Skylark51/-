import{GAME_CONFIG}from"../../data/game-config.js";import{BEAN_REWARDS}from"../../data/upgrades.js";import{TRAINING_MODES,getTrainingMode}from"../../data/training-modes.js";import{QUESTIONS,validateQuestions}from"../../data/questions.js";import{QuestionEngine}from"./question-engine.js";import{GameCore}from"./game-core.js";import{GameStorage}from"./storage.js";import{UpgradeSystem}from"./upgrade-system.js";import{ActionSystem}from"./action-system.js";import{UIAdapter}from"./ui-adapter.js?v=20260802-jar-rebuild1";const errors=validateQuestions();if(errors.length)throw new Error(`문항 데이터 오류: ${errors.join(", ")}`);const storage=new GameStorage,upgrades=new UpgradeSystem(storage),questionEngine=new QuestionEngine(QUESTIONS),actions=new ActionSystem({upgrades,storage}),game=new GameCore({questionEngine,config:GAME_CONFIG,upgradeSystem:upgrades,actionSystem:actions}),ui=new UIAdapter,requestedId=new URLSearchParams(location.search).get("training");let selectedTrainingId=getTrainingMode(requestedId)?.id||null,selectedDifficulty=storage.data.settings.difficulty||"normal",questionStartedAt=performance.now(),lastFrame=performance.now(),frameId=0;function saveRun(){if(["running","paused"].includes(game.state.status))storage.saveCurrentRun(game.snapshot())}function selectTraining(id){selectedTrainingId=getTrainingMode(id)?.id||null;const url=new URL(location.href);if(selectedTrainingId)url.searchParams.set("training",selectedTrainingId);else url.searchParams.delete("training");history.replaceState({},"",url)}function scheduleFrame(){if(frameId||game.state.status!=="running"||document.hidden)return;lastFrame=performance.now();frameId=requestAnimationFrame(frame)}function frame(now){frameId=0;if(game.state.status!=="running"||document.hidden)return;game.tick((now-lastFrame)/1e3);lastFrame=now;ui.render();scheduleFrame()}function start(options={}){if(!selectedTrainingId){ui.feedback("먼저 장독대를 선택해 주세요.","wrong");return}storage.startRun(selectedTrainingId,selectedDifficulty);game.start({trainingId:selectedTrainingId,difficulty:selectedDifficulty,...options});questionStartedAt=performance.now();saveRun();ui.clearAnswer();ui.render();scheduleFrame()}function submit(value){const result=game.submit(value??ui.answer());if(result.accepted){storage.recordAnswer(result.question,result.correct,false,performance.now()-questionStartedAt,game.state.difficulty);questionStartedAt=performance.now();saveRun();ui.clearAnswer();ui.render()}else if(result.reason==="empty")ui.feedback("정답을 입력해 주세요.","wrong")}ui.bind(game,{start,submit,restart:start});ui.installTrainingSelector(TRAINING_MODES.map(mode=>({...mode,bestScore:storage.getTrainingStats(mode.id).bestScore})),selectedTrainingId,selectTraining);ui.installDifficulty(selectedDifficulty,value=>{selectedDifficulty=value;storage.updateSettings({difficulty:value})});game.on("answer:timeout",detail=>{storage.recordAnswer(detail.question,false,true,performance.now()-questionStartedAt,game.state.difficulty);questionStartedAt=performance.now();saveRun()});game.on("answer:correct",detail=>{storage.recordWaterPour(detail.state.trainingId);saveRun()});game.on("answer:wrong",saveRun);game.on("game:pause",()=>{if(frameId)cancelAnimationFrame(frameId);frameId=0;saveRun()});game.on("game:resume",()=>scheduleFrame());game.on("fever:start",detail=>storage.recordFeverTier(detail.feverTier,detail.state.trainingId));game.on("game:over",detail=>{if(frameId)cancelAnimationFrame(frameId);frameId=0;storage.finishRun(detail.state)});game.on("game:clear",detail=>{if(frameId)cancelAnimationFrame(frameId);frameId=0;const previous=storage.getTrainingStats(detail.state.trainingId).bestScore;if(detail.state.score>previous){const bonus=actions.earn(BEAN_REWARDS.newHighScore,"new_high_score",detail.state.trainingId);detail.state.beansEarned=(detail.state.beansEarned||0)+bonus}storage.finishRun(detail.state)});document.addEventListener("visibilitychange",()=>{lastFrame=performance.now();if(document.hidden&&game.state.status==="running")game.pause();else scheduleFrame()});document.addEventListener("keydown",event=>{if(!event.ctrlKey&&!event.altKey&&!event.metaKey&&ui.chooseShortcut(event.key)){event.preventDefault();return}if(event.code==="Space"&&!["INPUT","TEXTAREA","SELECT","BUTTON"].includes(event.target?.tagName)){event.preventDefault();game.togglePause();ui.render()}});window.addEventListener("upgrade:purchased",()=>game.speak("upgradePurchased"));window.addEventListener("ui:device-mode",event=>storage.updateSettings({deviceMode:event.detail?.mode||"auto"}));window.addEventListener("beforeunload",()=>{if(frameId)cancelAnimationFrame(frameId);frameId=0;saveRun()});window.dispatchEvent(new CustomEvent("upgrades:loaded",{detail:{upgrades:upgrades.levels()}}));globalThis.KongJuiYaGame=Object.freeze({game,questionEngine,storage,upgrades,actions,TRAINING_MODES,start,submit,selectTraining,purchaseUpgrade:id=>upgrades.purchase(id),getUpgradeCards:shop=>upgrades.cards(shop)});